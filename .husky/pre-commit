#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Skip hooks in CI or if lint-staged is not available
if [ "$CI" = "true" ] || ! command -v npx &> /dev/null; then
  exit 0
fi

# Run lint-staged only if available
npx lint-staged || true